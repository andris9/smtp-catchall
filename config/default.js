'use strict';

module.exports = {
    // user: 'nobody',
    // group:'nogroup',

    greeting: 'Everything gets dumped and logged',
    port: 2525,
    host: '*',

    // allow messages up to 100MB
    maxSize: 100 * 1024 * 1024
};
